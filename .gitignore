# IDE
.idea/
*.iml

# Build output
target/

# OS junk
.DS_Store
Thumbs.db

# Secrets — the real properties file with credentials (NOT the .example)
jotpage.properties
src/main/resources/jotpage.properties

# Google OAuth credentials JSON (downloaded from Cloud Console)
client_secret_*.json
